#pragma once
#include <iostream>
#include <string>
#include <array>


class Piece{
	public:
		Piece();
		Piece(const std::string& name, const std::string& color, int* posArr[2]);
		~Piece();
		
		void setMName(const std::string& name);
		std::string& getMName();
		
		void setMColor(const std::string& color);
		std::string& getMColor();
	
		void dead();
		bool isAlive();
	
		void setMPosArr(int xpos, int ypos);
		int* getMPosArr();
		
	private:
		std::string mName;
		std::string mColor;
		int mPosArr[2]={0,0};
		bool mAlive = 1;
};